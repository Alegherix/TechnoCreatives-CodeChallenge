query Balloons($endCursor: ID, $filter: FilterInput) {
  balloons(after: $endCursor, filter: $filter) {
    edges {
      node {
        id
        name
        imageUrl
        description
        color
        variant
        price
        availableSince
      }
      cursor
    }
    pageInfo {
      startCursor
      endCursor
      hasPreviousPage
      hasNextPage
    }
  }
}
